faster-whisper>=1.0.0
jiwer>=3.0.0
scipy>=1.12.0
pandas>=2.0.0
matplotlib>=3.8.0
pyyaml>=6.0
soundfile>=0.12.0
requests>=2.31.0
torchaudio>=2.2.0
yt-dlp>=2024.0.0

# Kospeech (하한선 baseline)
# pip install kospeech  — 별도 설치 (torch 버전 의존성 주의)

# 한글 폰트 (시각화용 — 서버에 없으면 설치)
# apt-get install fonts-nanum  (Ubuntu)
