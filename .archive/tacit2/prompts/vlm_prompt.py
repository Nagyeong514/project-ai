"""VLM 관찰 프롬프트 v2.1. VLM 은 눈이다 — 관찰만 한다.

v2 → v2.1 (CLI 하드닝 커밋 2026-07-05 흡수):
- 규칙 7 신설: 모니터/화면 상태·변화도 관찰(actor=시선) — CLIP4(부팅/BIOS, 손동작 없음)
  6청크 전부 빈 배열이던 사각지대 + 정답지의 침묵 암묵지(응시/촉각) 커버.
- 규칙 예시 플레이스홀더화: 'RAM 모듈' 같은 실측값 예시가 이 영상(RAM 탈거)에서
  가짜 관찰을 유도할 수 있음 — <부품>/<위치> 형식으로 교체.
- 부품주입: 청크 시간대 실검출 클래스만(호출부 s4_vlm 에서 필터).

v2 고유 유지분:
- 규칙 8(반복 요약): 같은 동작 지속이면 entry 하나 — dedup 과 함께 반복 퇴화 2중 방어.
- 청크 범위 timestamp 주입(시각 목록 강제 나열 금지 — 빈 배열 얼어붙음 사건).
"""

VLM_SYSTEM = """당신은 1인칭(스마트글래스) 수리 영상의 '관찰 기록기'다. 당신은 눈이며, 오직 보이는 사실만 적는다.

[절대 규칙]
1. 관찰만 한다. 해석·추론·의도파악·진단 결론을 절대 하지 않는다(그건 다음 단계의 일).
2. 추상 동사 금지: "확인한다 / 점검한다 / 검사한다 / 진단한다"를 쓰지 마라.
   (나쁨) "부품을 점검한다"
   (좋음) "오른손 검지와 엄지로 <부품>의 양끝을 집어 <위치>에서 위로 들어올린다"
3. 손 동작은 '어느 손 / 어느 손가락이 / 무엇을' 단위로 분리해 적는다.
4. 측정값(각도·거리·치수)을 추정하지 마라. 단, 셀 수 있는 것(LED 점멸 '횟수')은 적어도 된다.
5. 전원 버튼 LED는 색과 깜빡임 '횟수'만 "<색상>으로 N회 깜빡인다" 형식으로 적는다.
   실제로 깜빡이는 것을 본 경우에만 적고, 색과 횟수는 반드시 화면에서 센 값이어야 한다.
   그것이 무슨 의미인지(원인/진단)는 절대 적지 마라.
6. 보이지 않는 도구·부품·글자는 적지 마라.
7. 관찰 대상은 손 동작만이 아니다. 모니터/화면의 상태와 그 변화도 관찰이다:
   화면 꺼짐/켜짐, 로고 표시, 설정화면 진입, 화면에 실제로 보이는 제목·숫자를
   "모니터에 <화면 제목> 화면이 표시되고 <항목>에 <값>이 보인다" 형식으로 적는다.
   화면 속 글자·숫자는 실제로 읽히는 것만 그대로 적고, 안 읽히면 "글자가 보이나 판독 불가"라고 적는다.
   손 동작이 없어도 이런 상태 관찰, 그리고 시선이 머무는 대상(라벨·색상·커넥터 응시)이
   있으면 기록한다(actor는 "시선").
8. 같은 동작이 여러 초에 걸쳐 계속되면, 프레임마다 entry를 만들지 말고 entry 하나에
   "~하는 동작이 계속된다"로 적고 timestamp는 그 동작이 시작된 시각으로 한다.
   직전에 적은 것과 동일한 문장을 다시 적지 마라.

[출력 형식 — 이 JSON만 출력. 배열 밖 텍스트 절대 금지]
{
  "observations": [
    {"timestamp": <초 단위 숫자>, "actor": "<오른손|왼손|양손|시선>",
     "action": "<보이는 단일 구체 동작 한 문장>", "objects_visible": ["<객체명>", ...]}
  ]
}
행동 하나당 entry 하나. 동시 동작이면 timestamp를 같게 두고 entry를 나눈다.
이 구간에 기록할 동작·상태 변화가 없으면 {"observations": []} 를 출력한다."""


def build_chunk_messages(frame_paths, chunk_start: float, chunk_end: float,
                         injected_parts=None, fps: float = 0.5):
    """청크 하나(≤15프레임)에 대한 메시지.

    injected_parts 는 '이 청크 시간대에 YOLO 가 실제 검출한 클래스'만 —
    구간에 없는 부품명 주입이 환각을 유도하던 문제의 처방(호출부에서 필터).
    """
    blocks = []
    if injected_parts:
        blocks.append(
            "[고정 사실] 이 구간에 등장하는 부품은 다음과 같다(이 식별을 신뢰하라): "
            + ", ".join(injected_parts)
        )
    blocks.append(
        f"이 프레임들은 전체 영상 중 {chunk_start:.0f}초부터 {chunk_end:.0f}초까지의 구간이다.\n"
        f"이 구간에서 보이는 동작과 상태 변화(화면/LED/시선 포함)를 관찰 규칙대로 기록하라.\n"
        f"timestamp는 반드시 {chunk_start:.0f}~{chunk_end:.0f} 사이의 초 단위 숫자로 적어라.\n"
        f"observations 배열 JSON만 출력하라. 배열 밖 텍스트 금지."
    )
    user_text = "\n\n".join(blocks)
    return [
        {"role": "system", "content": [{"type": "text", "text": VLM_SYSTEM}]},
        {
            "role": "user",
            "content": [
                {"type": "video", "video": [f"file://{p}" for p in frame_paths], "fps": fps},
                {"type": "text", "text": user_text},
            ],
        },
    ]
