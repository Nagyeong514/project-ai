# 이 폴더는 VLM 선정 실험의 **부분본**입니다

원래 `project-ai/STEP2_모델선정`에 있던 것을 03_VLM 카테고리로 옮겨왔다.

## 무엇인가
VLM 3종(Qwen3-VL-8B / InternVL3.5-8B / MiniCPM-V-4.5)을 동일 프레임·공통 프롬프트로
비교하려던 선정 실험. `inference/`에 3종 추론 스크립트 + `compare_results.py` + `prompts/common_prompt.md`.

## 왜 "부분본"인가
- `results/`에 **`qwen3vl/` 결과만** 있음(CLIP1~4, 242프레임). InternVL3.5·MiniCPM 결과 없음.
- `run_all.sh` 비어 있음, 채점·비교 보고서 없음. → 3종 비교 설계인데 Qwen3-VL만 실행된 상태.

## 완전판 위치
같은 VLM 3종 비교의 **완성본**(3모델 결과 전부 + 채점 + 보고서)은:
→ `../01_VLM모델실험/vlm_compare/` (같은 03_VLM 카테고리 안)
