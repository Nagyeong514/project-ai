# STEP8_RAG서비스 — 서시은(voice-rag-upload) 스택 그대로.
# torch/transformers는 시스템 site-packages에 이미 있음(--system-site-packages venv로 재사용).
fastapi
uvicorn[standard]
qdrant-client
sentence-transformers
requests
pydantic>=2.0.0
# ── 음성 계층 (2026-07-08 서시은 voice-rag v2 역머지, 버전은 v2 requirements.txt 고정값) ──
scipy==1.18.0
uroman==1.3.1.1
supertonic==1.3.1
faster-whisper==1.2.1
onnxruntime==1.27.0
python-multipart  # /transcribe의 UploadFile(Form data)에 필요 — v2 requirements엔 누락돼 있었음
# 주의: torchcodec은 재설치하지 말 것 (v2 HANDOFF.md — 기존 환경에서 문제 있었음)
chromadb  # 2026-07-12 백엔드 전환(vector_backend=chroma)
